#!/bin/bash

# Retrieve the Account Name (alias) and handle errors
ACCOUNT_NAME=$(aws iam list-account-aliases --query "AccountAliases[0]" --output text 2>/dev/null) &&
  [ -n "$ACCOUNT_NAME" ] || {
    echo "Error: Failed to retrieve Account Name or no account alias is set. Please check your AWS CLI configuration and permissions, or set an account alias."
    exit 1
  }

# Output the results
echo "Account ID: $ACCOUNT_ID"
echo "Account Name: $ACCOUNT_NAME"